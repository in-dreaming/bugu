# T004 P0 Zig 设备后端实现

状态：DONE  
类型：Implementation  
优先级：P0  
依赖：T002，T003  
预计产物：Zig backend 实现、最小播放 demo、build.zig/submodule 集成、验证记录。

## 1. 背景

P0 需要尽快稳定发声，但不能把 mixer 设计绑死在某个第三方库上。当前候选是 miniaudio；如果 T001 决定改用 SDL，则必须使用 in-dreaming/SDL.git enjin/gpu/main。无论选择哪个后端，Bugu 侧实现必须是 Zig，第三方库必须以 git submodule 引入。

## 2. 必读

- docs/tasks/asetup.md
- T002 运行时 contract
- T003 Zig API / 可选 C ABI 设计
- T001 中 backend 和 submodule 的调研结论

## 3. 实现范围

必须实现：

- Zig backend adapter。
- 第三方依赖的 git submodule 接入。
- build.zig 中的依赖编译/链接配置。
- device open/start/stop/close。
- callback 到 fixed quantum render 的适配。
- stereo float32 输出。
- sample rate 和 buffer size 配置。
- device error 和 basic telemetry。
- 一个最小 sine wave 或 test tone demo。

## 4. 不需要实现

- 完整 mixer。
- streaming。
- 声学传播。
- native WASAPI/CoreAudio/AAudio。

## 5. 验收标准

- demo 能通过真实设备 backend 播放稳定 test tone 至少 5 分钟；如果当前环境无音频设备，可额外提供 offline backend 生成真实 PCM/WAV 作为 CI fallback，但不能替代真实设备验收。
- 停止和销毁不崩溃、不泄漏明显资源。
- callback 内不分配、不锁、不做文件 I/O。
- 能记录 callback count、underrun/dropout 指标。
- 第三方类型不泄漏到公共 Zig API 或可选 C ABI。
- 如果使用 SDL，确认来源是 in-dreaming/SDL.git enjin/gpu/main。
- 依赖必须可通过 git submodule init/update 恢复。
- Evidence 必须包含：submodule 状态、zig build 命令、运行命令、5 分钟运行摘要或 offline PCM/WAV 输出摘要。
- 更新 docs/tasks/tasks.md 和本任务 Evidence。

## 5.1 禁止 mock

- 不能用空 callback 冒充播放成功。
- 不能调用系统播放器、外部命令或第三方 demo 冒充 Bugu backend。
- offline backend 必须走同一个 render callback/fixed quantum 适配路径，输出真实 sample buffer。

## 6. 验证建议

- 默认设备播放。
- 改 sample rate 和 period frames。
- start/stop 重复 100 次。
- 在 callback 中记录最大耗时，但不能格式化日志。

## 7. Deliverables

- `build.zig`：Zig 0.16 build，包含 `bugu_audio` static library、unit tests、`bugu-tone` demo、miniaudio C shim 编译和平台链接配置。
- `.gitmodules` + `third_party/miniaudio`：miniaudio git submodule，pinned at `9634bedb5b5a2ca38c1ee7108a9358a4e233f14d` (`0.11.25`) per T001.
- `src/bugu_audio.zig`、`src/core/engine.zig`、`src/device/device.zig`、`src/mixer/tone_renderer.zig`：P0 Zig backend/runtime slice with public API free of miniaudio types, offline backend, miniaudio backend, fixed-quantum adapter, and telemetry counters.
- `third_party_adapters/miniaudio/miniaudio_impl.c`: Bugu-owned miniaudio implementation translation unit.
- `examples/tone_demo.zig`: minimal tone demo with `--offline` and `--device` modes.

## 8. Evidence

- Submodule status:
  - `git submodule status --recursive`
  - Output: `9634bedb5b5a2ca38c1ee7108a9358a4e233f14d third_party/miniaudio (0.11.22-173-g9634bedb)`
- Build/test command:
  - `zig build test`
  - Result: passed.
- Offline render command:
  - `zig build tone -- --offline --seconds 2 --out bugu-tone.wav`
  - Output summary: `callbacks=375 frames=96000 quantums=375 underruns=0 dropouts=0 max_callback_ns=0 peak_abs=0.200000`
  - WAV output summary before cleanup: `E:\ws\infra\bugu\bugu-tone.wav`, `384044` bytes. This is a real stereo 48 kHz 16-bit PCM WAV generated by the Bugu offline backend through the same fixed-quantum adapter path.
- Real device smoke command:
  - `zig build tone -- --device --seconds 5`
  - Output summary: `callbacks=928 frames=237568 quantums=928 underruns=0 dropouts=0 max_callback_ns=0 peak_abs=0.200000`
- Real device 5-minute command:
  - `zig build tone -- --device --seconds 300`
  - Output summary: `callbacks=56121 frames=14366976 quantums=56121 underruns=0 dropouts=0 max_callback_ns=0 peak_abs=0.200000`
- Real implementation path:
  - miniaudio opens the default playback device with float32 stereo output.
  - miniaudio callback calls `MiniaudioBackend` -> `FixedQuantumAdapter.render` -> `Engine.render` -> `ToneRenderer.render`.
  - Offline backend also calls `FixedQuantumAdapter.render`; it does not use an external player or mock PCM.
  - `FixedQuantumAdapter` owns a preallocated pending buffer and adapts arbitrary callback frame counts to fixed 256-frame quantums without allocation.
- Realtime-safety note:
  - The miniaudio callback path does not allocate, lock, perform file I/O, format logs, or wait on GPU.
  - The callback writes only output samples and atomic telemetry counters.
- Limitation:
  - `max_callback_ns` is present in telemetry but remains `0` in this implementation because Zig 0.16 removed the old direct timestamp helper and this task avoids adding timing calls to the render callback. Callback count, rendered frames/quantums, underrun/dropout, and peak are recorded.

## 9. Activity Log

- 2026-07-07：任务创建。
- 2026-07-08：开始 T004，读取 T001/T002/T003 产物，选择 miniaudio P0 backend，并添加 pinned git submodule。
- 2026-07-08：实现 Zig build、offline backend、miniaudio backend、fixed-quantum adapter、tone demo 和 telemetry。
- 2026-07-08：修复 miniaudio `pUserData` lifetime bug，确保 callback points to stable backend instance。
- 2026-07-08：通过 `zig build test`、offline WAV render、5 秒真实设备 smoke 和 300 秒真实设备运行；状态置为 DONE。
